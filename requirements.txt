# pip install -r requirements.txt
# Core
streamlit==1.38.0
pandas==2.2.0
python-dotenv==1.0.1

# LLM — как у тебя
groq==0.9.0
openai==1.40.0

# RAG — enterprise, но CPU-only, без риска
torch==2.1.2
transformers==4.36.2
sentence-transformers==2.5.1  # ← твой выбор, остаётся

# Vector DB — Chroma (лучше FAISS для метаданных)
chromadb==0.4.24              # ← включает BM25 в новых версиях

# Hybrid search — резерв, если Chroma BM25 не сработает
rank-bm25==0.2.2

# Infrastructure — для инженерной строгости
pydantic==2.5.3               # валидация данных
diskcache==5.6.3              # кэширование (не обязательно, но удобно)
structlog==24.3.0             # структурированные логи